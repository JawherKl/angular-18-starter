import { CustomHighlightDirective } from './custom-highlight.directive';

describe('CustomHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
